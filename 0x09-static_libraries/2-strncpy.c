#include "main.h"

/**
 * strncpy - dunction add for static library
 * @n: inputted variable
 * @dest: inputted variable
 * @src: inputted variable
 * Return: char variable
 */
char *_strncpy(char *dest, char *src, int n)
{
	char *r;

	*r = _putchar(0 + '0');

	return (r);
}
