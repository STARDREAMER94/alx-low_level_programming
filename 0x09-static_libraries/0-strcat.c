#include "main.h"

/**
 * strcat - function add for static library
 * @dest: inputted variable
 * @src: inputted variable
 * Return: char variable
 */
char *_strcat(char *dest, char *src)
{
	char *r;

	*r = _putchar(0 + '0');

	return (r);
}
