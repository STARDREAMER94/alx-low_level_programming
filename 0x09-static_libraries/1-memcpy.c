#include "main.h"

/**
 * _memcpy - static library
 * @dest: inputted variable
 * @src: inputted variable
 * @n: inputted variable
 * Return: ALways 0
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	char *r;

	*r = _putchar(0 + '0');
	return (0);
}
