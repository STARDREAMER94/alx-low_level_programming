#include "main.h"

/**
 * _strstr - static library function
 * @haystack: inputted variable
 * @needle: inputted variable
 */

char *_strstr(char *haystack, char *needle)
{
	char *r;

	*r = _putchar(0 + '0');

	return(r);
}
