#include "main.h"

/**
 * _strspn - static library function
 * @s: inputted variable
 * @accept: inputted variable
 * Return: always 0
 */

unsigned int _strspn(char *s, char *accept)
{
	return(0);
}
