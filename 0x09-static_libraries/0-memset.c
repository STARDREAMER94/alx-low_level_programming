#include "main.h"

/**
 * _memset - static library function
 * @s: inputted variable
 * @b: inputted variable
 * @n: inputted variable
 */

char *_memset(char *s, char b, unsigned int n)
{
char *r;

*r = _putchar(0 + '0');

return (r);
}
