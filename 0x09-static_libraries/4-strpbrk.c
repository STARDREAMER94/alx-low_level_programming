#include "main.h"

/**
 * _strpbrk - static library function
 * @s: inputted variable
 * @accept: inputted variable
 * Return: always 0
 */

char *_strpbrk(char *s, char *accept)
{
	char *r;

	*r = _putchar(0 + '0');
	return(r);
}
